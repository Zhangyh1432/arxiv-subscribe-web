# Email credentials for sending notifications
# IMPORTANT: Do not use your primary email password.
# Generate an "App Password" from your email provider (e.g., Gmail, Outlook).
SENDER_EMAIL=your_sender_email@example.com
SENDER_PASSWORD=your_app_password
SMTP_SERVER=smtp.example.com
SMTP_PORT=587
RECIPIENT_EMAILS=recipient1@example.com,recipient2@example.com
EMAIL_SUBJECT=ArXiv Daily Papers

# --- API Service Endpoints ---

# LLM API Key from DashScope
DASHSCOPE_API_KEY=your_dashscope_api_key

# Base URL for the DashScope service (OpenAI compatible endpoint)
DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# URL for the local PDF parsing service
PDF_PARSER_URL=http://127.0.0.1:8000/file_parse